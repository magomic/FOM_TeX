Dies ist der Pfad in dem entwickelt wird, sobald eine Version vollständig ist, wird sie in das Verzeichnis (Repository) FOM_TeX kopiert und synchronisiert.

git add *
git commit -m "Version/Änderung etc."
git push